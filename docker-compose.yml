version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: karya_user
      POSTGRES_PASSWORD: karya_password
      POSTGRES_DB: karya_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U karya_user -d karya_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://karya_user:karya_password@db:5432/karya_db?schema=public
      - JWT_SECRET=super_secret_jwt_key_here
      - NODE_ENV=production
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
